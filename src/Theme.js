import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FF3644",
    },
    secondary: {
      main: "#7192A3",
    },
    action: {
      main: "#7192A3",
    },
  },
});

export default theme;
